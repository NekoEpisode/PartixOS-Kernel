package kr.partix.kernel.fs.vfs;

interface IFileOps {
    int read(FileDescriptor fd, byte[] buf, int len);
    int write(FileDescriptor fd, byte[] buf, int len);
    long seek(FileDescriptor fd, long offset, int whence);
    void close(FileDescriptor fd);
}
