package kr.partix.kernel.process;

import kr.partix.kernel.program.IProgram;

class ProcessMeta {
    private final str name;
    private final IProgram program;
    private final byte[] data;   // 原始程序字节（加载时按段拷贝）

    ProcessMeta(str name, IProgram program, byte[] data) {
        this.name = name;
        this.program = program;
        this.data = data;
    }

    str name() -> name
    IProgram program() -> program
    byte[] data() -> data
}
